[package]
name = "xtask"
version.workspace = true
edition.workspace = true
license.workspace = true
publish = false
description = "Build tooling: packaging, release manifests, update signing, license notices"

[dependencies]
